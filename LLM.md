# LLM

## Использование LLM при выполнении домашних заданий

### Используемая LLM

При возникновении сложностей или для проверки домашнего задания на корректность выполнения я иногда использовала **ChatGPT**. Она мне помогала быстро находить допущенные ошибки и реализовывать задуманное мной поведение программы (в отдельных местах).

#### Описание LLM
**ChatGPT** - это большая языковая модель, которая создавалась для работы с текстом. Она способна понимать, что хочет пользователь через запросы и выдавать (генерировать) ответы. Ответами могут быть как код, так и просто связный текст.

### Примеры использования

#### 1. Точечная помощь в реализации

##### Вопрос
Как прочитать данные из текстового файла в питоне?

##### Ответ
```
Вот кратко и по делу 👇

Как читать данные из файла в Python
1️⃣ Прочитать весь файл целиком
```
``` python
with open("data.txt", "r", encoding="utf-8") as file:
    data = file.read()

print(data)
```
```
Подходит, если файл небольшой.

2️⃣ Читать файл построчно (самый популярный вариант)
```
``` python
with open("data.txt", "r", encoding="utf-8") as file:
    for line in file:
        print(line.strip())
```

#### 2. Проверка соответствию критерию

##### Запрос
Проверь соответствует ли этот кусок критериям:
1. Отбор и фильтрация данных: - Выберите столбцы не менее 3 столбцов - Отфильтруйте строки фильтром на ваш выбор
``` bash
{r data_preparation}
data("mtcars")

filtered_data <- mtcars %>%
  select(mpg, cyl, hp, wt, am, gear) %>%
  filter(
    mpg >= 15,
    wt < 4
  )

cat("Осталось автомобилей после фильтрации:", nrow(filtered_data))
```

##### Ответ
```
Да, этот фрагмент полностью соответствует заданным критериям ✅
Разберём по пунктам.

1. Выбор столбцов (не менее 3)

В коде выбираются столбцы:
```
``` bash
select(mpg, cyl, hp, wt, am, gear)
```

```
➡️ Выбрано 6 столбцов, что больше минимально требуемых 3.
Критерий выполнен.
```
```
2. Фильтрация строк

Используется фильтрация:
```

``` bash
filter(
  mpg >= 15,
  wt < 4
)
```
```
➡️ Применены два осмысленных условия фильтрации:

по расходу топлива (mpg >= 15);

по весу автомобиля (wt < 4).

Фильтр выбран самостоятельно и логически обоснован.
Критерий выполнен.
```